# Design

This directory is reserved for historical product or component design notes.

Add a design document here when there is original vision, rejected alternative context, or a component specification worth preserving after implementation. Current gameplay and developer usage belong in `docs/guides/`.
